services:
  mailai:
    build: .
    container_name: gero-mailai
    ports:
      - "8005:8004"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - .:/app
    restart: unless-stopped
